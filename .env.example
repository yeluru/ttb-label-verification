# Anthropic API key used by the Claude Vision label extractor.
# Required in production; if absent the app falls back to MockProvider in development.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional. Defaults to the current Anthropic Sonnet model used for vision extraction.
ANTHROPIC_MODEL=claude-sonnet-4-6
